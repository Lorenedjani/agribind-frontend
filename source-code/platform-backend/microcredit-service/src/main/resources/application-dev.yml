spring:
  datasource:
    url: jdbc:mysql://localhost:3307/agribind_microcredit_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.microcredit: TRACE
    org.springframework.data.mongodb: DEBUG