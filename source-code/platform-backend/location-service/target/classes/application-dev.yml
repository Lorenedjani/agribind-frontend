spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cooperative_db_dev?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    '[com.agribind.location_service]': DEBUG