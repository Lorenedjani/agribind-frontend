server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://production-db:3306/productiondb
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
jwt:
  secret: ${JWT_SECRET:my-very-secret-key-change-me-please}
